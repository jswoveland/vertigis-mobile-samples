<?xml version="1.0" encoding="UTF-8"?>

<core:ContentComponent xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                       xmlns:core="clr-namespace:VertiGIS.Mobile.Workflow.Core;assembly=VertiGIS.Mobile.Workflow"
                       xmlns:views="clr-namespace:VertiGIS.Mobile.Toolkit.Views;assembly=VertiGIS.Mobile.Toolkit"
                       x:Class="VertiGIS.Mobile.Samples.Samples.Workflow.CustomFormComponent.CustomFormComponent"
                       xmlns:treeview="clr-namespace:Syncfusion.Maui.TreeView;assembly=Syncfusion.Maui.TreeView"
                       xmlns:treeviewengine="clr-namespace:Syncfusion.TreeView.Engine;assembly=Syncfusion.Maui.TreeView"
                       xmlns:checkbox="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
                       xmlns:local="clr-namespace:VertiGIS.Mobile.Samples.Samples.Workflow.CustomFormComponent"
                       >

        <treeview:SfTreeView x:Name="treeView" 
                             CheckBoxMode="Recursive"
                             ItemTemplateContextType="Node"
                             CheckedItems="{Binding CheckedNodes}"
                             ItemsSource="{Binding RootNodes}"
                             ItemTapped="treeView_ItemTapped"
                             ItemDoubleTapped="treeView_ItemDoubleTapped"
                             ExpanderWidth="0"
                             NodeSizeMode="Dynamic">

        <treeview:SfTreeView.HierarchyPropertyDescriptors>
            <treeviewengine:HierarchyPropertyDescriptor TargetType="{x:Type local:TreeNode}" ChildPropertyName="Children"/>
        </treeview:SfTreeView.HierarchyPropertyDescriptors>

        <treeview:SfTreeView.ItemTemplate>
            <DataTemplate>
                <Grid x:Name="grid" RowSpacing="0" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"  />
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" Grid.Row="0" Margin="0">
                        <Label Text="{Binding Content.NodeSymbol.Symbol}" FontFamily="Wingdings" TextColor="{Binding Content.NodeSymbol.Colour}" VerticalTextAlignment="Center" FontSize="Default" />
                        <!-- checkbox:SfCheckBox VerticalOptions="Center"
                         IsThreeState="True"
                        HorizontalOptions="Center"
                        IsChecked="{Binding IsChecked, Mode=TwoWay}"/ -->

                    </Grid>
                    <Grid Grid.Column="1" Grid.Row="0">
                        <Label LineBreakMode="NoWrap"
                               Text="{Binding Content.Name}"
                               VerticalTextAlignment="Center"
                               FontSize="Default">
                               
                        </Label>
                     </Grid>

                    <Grid Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="1">
                        <Entry IsVisible="{Binding Content.CommentState}" Text="{Binding Content.Comment}"  MaxLength="100" Placeholder="Comment" Completed="Entry_Completed" />
                        
                    </Grid>
                </Grid>
            </DataTemplate>
        </treeview:SfTreeView.ItemTemplate>

    </treeview:SfTreeView>  
</core:ContentComponent>
